export interface cineCreacionDTO {
    name: string;
    latitude: number;
    longitude: number;
}

export interface cineDTO {
    id:string;
    name: string;
    latitude: number;
    longitude: number;
}